doctype html
html(lang="en")
  head
    title cards
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    if useExternalStyles
      link(rel="stylesheet", href="/build/app.css")
  body
    #mount
      header
        h1 Un momento, por favor.
      .content
        h2 We're shuffling the decks...
    script(src=scriptRoot + "vendor.js")
    script(src=scriptRoot + "app.js")
