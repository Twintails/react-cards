/* eslint-disable no-unused-vars */
import "./stacks.scss"

import React from "react"
import Card from "./card"

export default function Stacks(props) {
  const {stacks, opSelectStack, selectStack, ourStack} = props
  return <p>{stacks, opSelectStack, selectStack, ourStack}</p>
}
